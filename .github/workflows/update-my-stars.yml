name: Update my stars
on:
  workflow_dispatch:
  schedule:
     - cron:  '12 3 * * 1'

jobs:
  Update-my-stars:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Re-generate README
        run: pipx run starred --username kissgyorgy --sort > README.md

      - name: Push changes
        run: |
          git config --global user.name 'Kiss György'
          git config --global user.email 'kissgyorgy@me.com'
          git add README.md
          git commit -m "Automatic update"
          git push
